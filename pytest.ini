[pytest]
# Where the tests live, so "pytest" with no arguments finds them.
testpaths = tests

# Show the name of each test as it runs, and a short summary of any that
# fail, rather than the default dots.
addopts = -v --tb=short
